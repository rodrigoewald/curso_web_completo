// PI * raio * raio

const PI = 3.141592;
const raio = 10;
let area = PI * (raio * raio);

console.log(
    "A área é " + area + ' M2'
);

area = Math.PI * (raio * raio);
console.log(
    "A área é " + area + ' M2'
)